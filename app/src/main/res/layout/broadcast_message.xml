<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@drawable/background"
    android:layout_height="match_parent">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/broadcast_msg"
        android:hint="What is your idea today ?"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Post"
        android:id="@+id/post_button"
        android:background="#3245AC"
        android:layout_marginTop="86dp"
        android:layout_below="@+id/broadcast_msg"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/broadcastSubject"
        android:hint="BroadCast Subject"
        android:layout_above="@+id/broadcast_msg"
        android:layout_alignStart="@+id/broadcast_msg" />


</RelativeLayout>